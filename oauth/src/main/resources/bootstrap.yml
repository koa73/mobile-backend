spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: http://${CONFIG_SERVICE:}:8001
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD:}
      username: user

app:
  secure:
     user: ${ACCOUNT_SERVICE_USER:}
     password: ${ACCOUNT_SERVICE_PASSWORD:}

