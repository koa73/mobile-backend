spring:
  mvc:
    locale: ru
  application:
    name: mobile
  cloud:
    config:
      uri: http://${CONFIG_SERVICE:web.phone4pay.ru}:${CONFIG_SERVICE_PORT:8001}
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD:}
      username: user

logging:
  level:
    ru.mobile.front.rest.exception.RestApiException: DEBUG
    //ru.mobile.front.config.I18nConfiguration: DEBUG


